import { Module } from "@nestjs/common";

/**
 * V2 API Module (Placeholder)
 * Routes are prefixed with /api/v2
 *
 * When you need v2 endpoints with different response shapes or new features,
 * import feature modules here. Share business logic from v1 services.
 */
@Module({
  imports: [],
})
export class V2AppModule {}
