# Environment variables for the debridgers-frontend application
VITE_API_URL=http://localhost:4001/api/v1
NODE_ENV=development
PORT=5173


# Must match REQUEST_KEY in the backend .env - apiFetch sends it as X-Request-Key
# on authenticated calls, and RequestKeyGuard rejects the request without it.
VITE_REQUEST_KEY=""
