{
  "name": "debridgers-agent",
  "root": "apps/debridgers-agent",
  "sourceRoot": "apps/debridgers-agent/app",
  "projectType": "application",
  "tags": ["scope:frontend", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{projectRoot}/build"],
      "options": {
        "command": "pnpm run build",
        "cwd": "apps/debridgers-agent"
      }
    },
    "build:staging": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{projectRoot}/build"],
      "options": {
        "command": "pnpm run build:staging",
        "cwd": "apps/debridgers-agent"
      }
    },
    "build:production": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{projectRoot}/build"],
      "options": {
        "command": "pnpm run build:production",
        "cwd": "apps/debridgers-agent"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run dev",
        "cwd": "apps/debridgers-agent"
      }
    },
    "dev:staging": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run dev:staging",
        "cwd": "apps/debridgers-agent"
      }
    },
    "dev:production": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run dev:production",
        "cwd": "apps/debridgers-agent"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run start",
        "cwd": "apps/debridgers-agent"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run lint",
        "cwd": "apps/debridgers-agent"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run typecheck",
        "cwd": "apps/debridgers-agent"
      }
    }
  }
}
