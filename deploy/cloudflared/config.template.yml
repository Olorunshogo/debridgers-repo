# === Stood down
# Deployment is deferred, so this file is commented out in full. Nothing reads
# it while deploy/deploy.sh is inert. Uncomment to bring it back.

# # Rendered to deploy/cloudflared/config.yml by deploy.sh, which substitutes the
# # tunnel id and hostname from the host .env. Do not edit config.yml by hand: the
# # next deploy overwrites it.
#
# tunnel: ${TUNNEL_ID}
# credentials-file: /etc/cloudflared/credentials.json
#
# # The DNS record for API_HOSTNAME must be a CNAME to <TUNNEL_ID>.cfargotunnel.com
# # or the tunnel comes up healthy and still serves nothing.
# ingress:
#   - hostname: ${API_HOSTNAME}
#     service: http://backend:4001
#   # cloudflared refuses to start without a catch-all rule.
#   - service: http_status:404
#
