import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        colors: {
          primary: "#0ea5e9",
          secondary: "#6366f1",
        },
      },
    },
  },
  plugins: [],
};

export default config;
